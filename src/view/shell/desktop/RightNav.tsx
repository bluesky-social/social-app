// Eurosky: the desktop right sidebar is intentionally cleared. This component
// renders nothing so the layout reclaims the space.
export function DesktopRightNav(_props: {routeName: string}) {
  return null
}
