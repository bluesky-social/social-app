import {createBottomTabNavigator} from '@react-navigation/bottom-tabs'

import {type BottomTabNavigatorParams} from './lib/routes/types'

export const Tab = createBottomTabNavigator<BottomTabNavigatorParams>()
