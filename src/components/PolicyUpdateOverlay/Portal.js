import { createPortalGroup } from '#/components/Portal';
var portalGroup = createPortalGroup();
export var Provider = portalGroup.Provider;
export var Portal = portalGroup.Portal;
export var Outlet = portalGroup.Outlet;
