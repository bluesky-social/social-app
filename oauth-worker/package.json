{
  "name": "eurosky-oauth-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Stateless Cloudflare Worker that signs Eurosky's private_key_jwt OAuth client assertions. Holds the confidential client private key; stores nothing.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
