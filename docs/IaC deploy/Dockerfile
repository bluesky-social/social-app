# Usa la imagen base 
FROM public.ecr.aws/t8b2r8w9/social-app:latest

# Expone el puerto 8100 que es el puerto en el que el servidor escucha
EXPOSE 8100

# Comando que se ejecutará al iniciar el contenedor
CMD ["/usr/bin/bskyweb", "serve"]
